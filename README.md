# filmscope
Movie Recommendation System

Please install the below python dependencies to run.
requests
firebase-admin
python-dotenv

sample command - pip install requests or python install request and similar for other dependencies.